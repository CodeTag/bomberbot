<div class="alert alert-block">
  <h4 class="alert-heading">¡ATENCI&Oacute;N!</h4>
  <br>
  Leaderboard definitivo, competencia Campus-party
</div>

<table class="table">
  <thead>
    <tr>
      <th>Posicion</th>
      <th>Usuario</th>
      <th>Puntos</th>
      <th>Partidas</th>
    </tr>
  </thead>
  <tbody>
    <% users.forEach(function(user, index){ %>
    <tr>
      <td><%= index+1 %></td>
      <td><a href=<%= '/users/games/'+user.get('username') %> ><%= user.get('username') %></a></td>
      <td><%= user.get('totalPrueba') %></td>
      <td><%= user.get("partidasJugadas") %></td>
    </tr>
    <% }) %>
  </tbody>
</table>